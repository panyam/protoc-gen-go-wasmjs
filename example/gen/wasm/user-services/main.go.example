// Example main.go for user_services WASM module
// Copy this file to your project and customize the service implementations
//
// To use:
// 1. Run `buf generate` to generate the WASM package (user_services.wasm.go)
// 2. Copy this file to your project and update the import path below
// 3. Implement your services and build with: GOOS=js GOARCH=wasm go build -o user_services.wasm

package main

import (
	// Import the generated WASM package - UPDATE THIS PATH
	"your-module/path-to-generated/user_services"
	
	// Import the same protobuf types as the generated WASM file
	libraryv1 "github.com/panyam/protoc-gen-go-wasmjs/example/gen/go/library/v1"
)

func main() {
	// Initialize service implementations with your business logic
	exports := &user_services.User_servicesServicesExports{
		// TODO: Replace with your actual UserService implementation
		// UserService: &yourUserServiceImplementation{
		//     // Initialize with database, config, etc.
		//     db: database,
		//     auth: authService,
		//     config: appConfig,
		// },
	}
	
	// Register the JavaScript API
	exports.RegisterAPI()
	
	// Keep the WASM module running
	select {}
}