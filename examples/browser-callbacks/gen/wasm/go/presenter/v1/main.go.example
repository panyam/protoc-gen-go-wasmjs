// Example main.go for browser_callbacks WASM module
// Copy this file to your project and customize the service implementations
//
// To use:
// 1. Run `buf generate` to generate the WASM package (browser_callbacks.wasm.go)
// 2. Copy this file to your project and update the import path below
// 3. Implement your services and build with: GOOS=js GOARCH=wasm go build -o browser_callbacks.wasm

package main

import (
	// Import the generated WASM package - UPDATE THIS PATH
	"your-module/path-to-generated/browser_callbacks"
	
	// Import the same protobuf types as the generated WASM file
	presenterv1 "github.com/panyam/protoc-gen-go-wasmjs/examples/browser-callbacks/gen/go/presenter/v1"
	wasm "github.com/panyam/protoc-gen-go-wasmjs/pkg/wasm"
)

func main() {
	// Initialize service implementations with your business logic
	exports := &browser_callbacks.Browser_callbacksServicesExports{
		// TODO: Replace with your actual PresenterService implementation
		// PresenterService: &yourPresenterServiceImplementation{
		//     // Initialize with database, config, etc.
		//     db: database,
		//     auth: authService,
		//     config: appConfig,
		// },
	}
	
	// Register the JavaScript API
	exports.RegisterAPI()
	
	// Keep the WASM module running
	select {}
}