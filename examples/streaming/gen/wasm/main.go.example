// Example main.go for streaming_service WASM module
// Copy this file to your project and customize the service implementations
//
// To use:
// 1. Run `buf generate` to generate the WASM package (streaming_service.wasm.go)
// 2. Copy this file to your project and update the import path below
// 3. Implement your services and build with: GOOS=js GOARCH=wasm go build -o streaming_service.wasm

package main

import (
	// Import the generated WASM package - UPDATE THIS PATH
	"your-module/path-to-generated/streaming_service"
	
	// Import the same protobuf types as the generated WASM file
	streamingv1 "github.com/panyam/protoc-gen-go-wasmjs/examples/streaming/gen/go/streaming/v1"
)

func main() {
	// Initialize service implementations with your business logic
	exports := &streaming_service.Streaming_serviceServicesExports{
		// TODO: Replace with your actual TickService implementation
		// TickService: &yourTickServiceImplementation{
		//     // Initialize with database, config, etc.
		//     db: database,
		//     auth: authService,
		//     config: appConfig,
		// },
	}
	
	// Register the JavaScript API
	exports.RegisterAPI()
	
	// Keep the WASM module running
	select {}
}